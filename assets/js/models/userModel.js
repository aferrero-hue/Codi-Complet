/*const mongoose = require('mongoose');

//És un exemple per realitzar proves, s'ha de modificar:
const userSchema = new mongoose.Schema({
    name: String,
    email: String
});

module.exports = mongoose.model('User', userSchema);*/